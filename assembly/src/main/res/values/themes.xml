<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="StartingTheme" parent="Theme.SplashScreen.IconBackground">
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher</item>
        <item name="postSplashScreenTheme">@style/Theme.GroceryList</item>
        <item name="windowSplashScreenBackground">@color/background</item>
    </style>

    <style name="Theme.GroceryList" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>

</resources>
